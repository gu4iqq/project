<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Учет пропущенных занятий</title>
    <link type="image/png" sizes="320x320" rel="icon" href="./images/icon.png" />
    <link rel="stylesheet" type="text/css" href="./style.css" />
  </head>
  <body id="body">
    <div class="container">
      <%- include('layouts/header') -%>

      <div class="content">
        <p>Id <%= user.id %>, Имя: <%= user.username %>, Роль: <%= user.role %></p>

        <form class="form_center" action="/changeRole" method="post">
          <input type="text" name="userId" id="userId" value="<%= user.id %>" style="display: none" />
          <select id="role" name="role">
            <option value="USER" selected>Пользователь</option>
            <option value="PREPOD">Преподаватель</option>
            <option value="ADMIN">Администратор</option>
          </select>
          <button class="button">Сменить роль</button>
        </form>

        <form class="form_center" action="/deleteUser" method="post">
          <input type="text" name="id" id="id" value="<%= user.id %>" style="display: none" />
          <button class="button">Удалить пользователя</button>
        </form>
      </div>

      <%- include('layouts/footer') -%>
    </div>
  </body>
</html>
